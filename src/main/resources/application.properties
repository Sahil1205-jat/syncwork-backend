# Aiven Cloud Database Configuration
spring.datasource.url=jdbc:postgresql://syncwork-sahilsepat2-559b.c.aivencloud.com:12835/defaultdb?sslmode=require
spring.datasource.username=avnadmin
# The password will be read from an environment variable named DB_PASSWORD
spring.datasource.password=${DB_PASSWORD}

# --- Development Settings ---
# 'update' will automatically update the database schema (add tables/columns) without deleting data.
# This is good for development but should be changed to 'validate' for production.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Email Configuration ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=syncwork0@gmail.com
spring.mail.password=omau sleh aygy yzui
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
